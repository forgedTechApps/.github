name: Reusable — Security Scan (minimal test)

on:
  workflow_call:
    inputs:
      language:
        description: 'CodeQL language'
        type: string
        default: 'javascript-typescript'
        required: false
      enable-zap:
        description: 'Run OWASP ZAP baseline scan'
        type: boolean
        default: false
        required: false
    secrets:
      NVD_API_KEY:
        required: false

jobs:
  codeql:
    name: CodeQL (${{ inputs.language }})
    if: inputs.language != 'swift'
    runs-on: ubuntu-latest
    timeout-minutes: 30

    permissions:
      contents: read
      security-events: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Echo test
        run: echo "CodeQL would run here for ${{ inputs.language }}"

  codeql-swift:
    name: CodeQL (swift)
    if: inputs.language == 'swift'
    runs-on: macos-latest
    timeout-minutes: 30

    permissions:
      contents: read
      security-events: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Echo test
        run: echo "CodeQL Swift would run here"

  dependency-audit:
    name: Dependency Audit
    runs-on: ubuntu-latest
    timeout-minutes: 30

    permissions:
      contents: read
      security-events: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Echo test
        run: echo "Dependency audit would run here for ${{ inputs.language }}"
