# Handoff: org-wide standards hardening + veda dev/main reconcile — 2026-06-05

**Goal:** Harden the forgedTechApps agent-standards framework and propagate it to all
consuming repos; reconcile veda's diverged dev/main and ship the framework via `v1`.
"Done" = all PRs merged, `v1` at current main, no broken consumer CI.

**State:**
- ✅ `check_framework_support` MCP tool — runtime EOL check (forgedtech #68, merged)
- ✅ Verbosity/output-discipline rolled out org-wide: inherited `output_discipline` rule + `SessionStart` hook wired in kurata/veda/Viyr/forgedtech (eleven11 already had it); CLAUDE.md prose added to Viyr/TradingBot (forgedtech #69, kurata #180, Viyr #107, all merged)
- ✅ `grill-me` → `interview-me` rename completed in all live CLAUDE.md + fixed TradingBot's dangling skill symlink
- ✅ interview-me **branch 8** — per-task dispatch recommendation (inline/one/many), inline-default (forgedtech #71, merged)
- ✅ eleven11 #53 — partner-chart cap + swisseph ephe-path fix (merged by a parallel session; branch cleaned)
- ✅ veda dev↔main **reconciled** — #25 (test fixes) + #26 (merge main↔dev) merged; main now has working Swift CI for the first time
- ✅ `v1` tag moved to current main (`1d97d8b`) — all consumers now have the full framework
- ⬜ Nothing in flight. All session tasks complete.

**Open PRs / branches:**
- **No open PRs in any org repo.** (forgedtech, kurata, viyr-app, veda, eleven11, veda-proxy all clear.)
- veda clone `/Users/dev/Development/veda/veda` is on `fix/flaky-clock-tests-23`; stale local branches there: `ci/fix-and-verify-v1test`, `ci/gate-v1test-to-v1` (pre-existing, not this session — leave unless asked).
- forgedtech on `main` (synced). The temp `veda-rebase-wt` worktree + `chore/rebase-dev-onto-main` branch were removed.

**Open decisions (block progress):**
- none blocking. Deferred by user earlier: forgedtech #41 (publish MCP to npm / retire `AGENT_STANDARDS_HOME`); pen-test MCP tool (decided NOT to build — not a binary check).

**Key files (entry point first):**
- `agent-standards/skills/interview-me/SKILL.md` — branch 8 (dispatch recommendation) is the newest change; canonical skill, symlinked into every repo
- `mcp-server/src/check-framework-support.ts` — the EOL check; 3 surfaces synced in `mcp-server/src/server.ts` + `standards.ts` + `agent-standards/schema/agent-standards.schema.json`
- `mcp-server/templates/defaults/org-defaults.yml` — the `framework_support:` EOL table + `output_discipline`/dispatch rules
- `agent-standards/hooks/session-start.mjs` — emits the output-discipline reminder (wired via each repo's `.claude/settings.json`)

**Gotchas:**
- **veda `dev` is now stale vs `main`** (`main...dev` = ahead 18 / behind 51). #26 was *squash*-merged, so dev's commits show as "ahead" but their CONTENT is in main (verified: `scheme: 'Veda'` + the #25 fix are on main). To avoid re-diverging, **reset/fast-forward veda `dev` to `main`** before doing more dev work. Do NOT re-merge dev→main as-is — it would look diverged.
- **swiftlint `--fix` is a footgun across a merge**: applying dev's stricter config to main's code produced 5 lint-clean-but-non-compiling breaks (trailing-closure `}??`, `.isEmpty` on a custom struct, ViewBuilder bare `_ =`, `catch let e` binding loss, dropped `Date??` flatten). **Always `xcodebuild build-for-testing` after a swiftlint --fix on merged code** — lint passing ≠ compiles.
- Moving the org-wide `v1` tag is classifier-gated (deploys to every consumer); needs explicit user authorization each time. CLAUDE.md requires coordination before a v1 bump.
- veda `/Users/dev/Development/veda` is NOT a git repo — it's a parent of 3 separate repos (`veda/`, `veda-ios/`, `veda-proxy/`). Its root `CLAUDE.md`/`.claude/settings.json` are un-versioned (on-disk only) by existing convention.

**Verification:**
- Framework shipped: `gh api repos/forgedTechApps/.github/git/refs/tags/v1 --jq .object.sha` == `…/heads/main` → both `1d97d8b…` (✓ verified)
- No pending merges: `for r in .github kurata viyr-app veda eleven11; do gh pr list --repo forgedTechApps/$r --state open; done` → empty (✓ verified)
- veda main is sound: Swift Quality Gate green on #26 (11m), `xcodebuild build-for-testing` scheme Veda → `** TEST BUILD SUCCEEDED **`, swiftlint --strict 0 violations

**Next concrete step:**
None required — session work is complete. If resuming veda: `git -C /Users/dev/Development/veda/veda fetch && git branch -f dev origin/main && git push --force-with-lease origin dev` to re-sync dev to the reconciled main (coordinate first — it rewrites dev). Otherwise the backlog items (#41 MCP publish; viyr/eleven11 product issues) are the only remaining org work.
